% Suggested caption - the numbers are generated, so edit the prose only.
\caption{Energy saved by translating Python to Go, over 1000 invocations, for the
  46 validated translations of run \texttt{20260904-190539} (RAPL, steady
  state). Negative values mean the Python version was more energy efficient, as
  in Werner et al.\ Figure~1, whose bars are reproduced as the dashed outline
  in~(a). The saving is confined to the AWS functions (median $21.7$\,J,
  $21/23$ positive, sign test $p=7 \times 10^{-5}$); for the non-AWS functions it is
  indistinguishable from zero (median $-0.001$\,J, $10/23$ positive,
  $p=0.68$). The two groups differ at Mann--Whitney $p=1 \times 10^{-5}$.}
