% Suggested caption - the numbers are generated, so edit the prose only.
\caption{Break-even invocations per function across the three runs of the replicate series. (a) Each row is one of the
  43 functions that repay in at least one run in which they were validated; markers
  give its break-even in each run (circle \texttt{20260904-190539}, square \texttt{20260911-165103}, triangle \texttt{20260912-172904}), a cross at the right a run in which its
  validated translation is not faster than Python, and a missing marker a run in which it
  was not validated. Bars span the runs, black ticks mark the median. For the 27 functions that repay in at least two runs, the highest and lowest value differ by a median factor of 1.6 (all within a factor of ten); 10 functions repay in one run and never in another. A further
  12 functions are validated but never repay in any run. (b) The repaying
  translations of each run and, last, the per-function medians; boxes are quartiles on the logarithmic scale, whiskers
  the extremes, and the counts give the repaying and the never-repaying translations. The
  medians are \texttt{20260904-190539} 1.0$\times$10\textsuperscript{6}; \texttt{20260911-165103} 4.5$\times$10\textsuperscript{5}; \texttt{20260912-172904} 1.3$\times$10\textsuperscript{5}; per-function median 4.5$\times$10\textsuperscript{5}.}
