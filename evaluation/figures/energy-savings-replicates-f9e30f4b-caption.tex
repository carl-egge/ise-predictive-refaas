% Suggested caption - the numbers are generated, so edit the prose only.
\caption{Energy saved by translating Python to Go, over 1000 invocations, for the
  55 functions validated in at least one of the three runs of the replicate series, each at the median of its saving over the runs in which it was validated (RAPL, steady state). Negative values mean the Python version was more
  energy efficient, as in Werner et al.\ Figure~1, whose bars are reproduced as the
  dashed outline in~(a). The saving is confined to the AWS functions (median
  $34.4$\,J, $27/29$ positive, sign test $p=2 \times 10^{-6}$); for the non-AWS functions it
  is indistinguishable from zero (median $-0.013$\,J, $12/26$ positive,
  $p=0.85$). The two groups differ at Mann--Whitney $p=3 \times 10^{-7}$. Per run, the AWS median is 21.7, 28.0 and 57.6\,J.}
